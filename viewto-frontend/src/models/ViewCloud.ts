export class ViewCloud {
    id: string;
    references: string[];

    /**
     *
     */
    constructor(id: string, references: string[]) {
        this.id = id;
        this.references = references;
    }
}

